{% extends 'base.html.twig' %}


{% block body %}

    <div class="container">

        <center>
            <h1 class="mt-5">{{ "Users list"|trans}}</h1>
        </center>

        <div class="row">
            {% for user in users%}
                <div class="col-md-4">
                    {% include 'user/_users.html.twig' %}
                </div>
            {% endfor %}
        </div>

        <div class="pagination">
            {{ knp_pagination_render(users) }}
        </div>

    </div>
{% endblock %}

